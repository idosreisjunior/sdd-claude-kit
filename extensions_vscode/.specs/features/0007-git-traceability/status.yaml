# Estado da mudança. Forma válida: plugins/sdd-kit/schemas/status.schema.json
#
# Datas SEMPRE entre aspas: sem elas o YAML produz um objeto data e a
# validação por schema falha. Ver standards.md §9.
version: 1

id: "0007-git-traceability"
# feature | bug | refactor | change
type: "feature"
title: "Git e rastreabilidade"

# Os três incrementos (adapter+escopo, navegação, sugestões) estão implementados: a
# feature cobre os RF-014/015/018. Segue IN_PROGRESS até a etapa de verify (Fase 2).
status: VERIFIED

created: "2026-07-31"
updated: "2026-07-31"

# Histórico de transições. Só acrescenta; nunca reescreve.
history:
  - status: DRAFT
    date: "2026-07-31"
    reason: >-
      Materializada a partir do backlog (Épico 7) a pedido do usuário, para
      especificar e implementar. spec.md rascunhada a partir dos RF-014, RF-015 e
      RF-018 do PRD, com as decisões de design registradas como questões abertas.
  - status: DRAFT
    date: "2026-07-31"
    reason: >-
      Spec detalhada (REQ-TRACE-001..005, NFR-TRACE-001..004, SCN-TRACE-001..008).
      As cinco questões Q1–Q5 foram respondidas pelo autor e registradas na seção
      "Decisões de escopo" (D-Q1..D-Q5); o bloqueio crítico Q1 (acesso ao Git via
      binário + parser puro) foi encerrado. Primeiro incremento definido.
  - status: IN_PROGRESS
    date: "2026-07-31"
    reason: >-
      Incremento 1 implementado a pedido do autor, fora da ordem formal do fluxo
      (sem approve/design da Fase 1). Adapter de Git somente-leitura (ADR-011):
      núcleos puros gitParse.ts, scopeCheck.ts e tasksPlan.ts (12 testes novos),
      borda gitAdapter.ts, comando "Verificar escopo" e config sddClaudeKit.scope.*.
      Verificação: compile, lint e 91 testes limpos, mais teste ponta-a-ponta do
      adapter contra o repo real. Transição DRAFT → IN_PROGRESS (pulados
      CLARIFIED/DESIGNED/PLANNED/APPROVED da Fase 2) confirmada pelo autor.
      Faltam os incrementos REQ-TRACE-004 (navegação) e REQ-TRACE-005 (sugestões).
  - status: IN_PROGRESS
    date: "2026-07-31"
    reason: >-
      Incremento 2 implementado: navegação de rastreabilidade (REQ-TRACE-004,
      SCN-TRACE-007) por QuickPick (D-Q6). Núcleo puro traceabilityNav.ts (4 testes
      novos) e comando "Navegar rastreabilidade" — abre spec.md/tasks.md/arquivo ou
      busca o teste, somente leitura. Verificação: compile, lint e 95 testes limpos.
      Falta apenas o incremento REQ-TRACE-005 (sugestões de branch/commit).
  - status: IN_PROGRESS
    date: "2026-07-31"
    reason: >-
      Incremento 3 implementado: sugestões de branch/commit (REQ-TRACE-005,
      SCN-TRACE-008) por diálogo com botões Copiar (D-Q7) — nunca executa git.
      Núcleo puro commitSuggest.ts (3 testes novos) e comando "Sugerir commit".
      Verificação: compile, lint e 98 testes limpos. A 0007 cobre agora os três RFs
      (014/015/018): feature-completa, pronta para verify. Segue IN_PROGRESS.
  - status: VERIFIED
    date: "2026-08-01"
    reason: >-
      Verificada a pedido do usuário. Feature-completa (RF-014/015/018), com os três comandos
      funcionais na extensão empacotada (0.0.7) e instalada. Núcleos puros cobertos por testes;
      verificação global verde (compile, lint, 115 testes). Requisitos de borda verificados por
      revisão manual no host. Transição IN_PROGRESS → VERIFIED fora da ordem formal (approval
      segue null; approve é da Fase 2), confirmada pelo usuário.

# null significa NÃO APROVADO. Preenchido por /sdd-kit:approve com
# {date, by, revision}.
approval: null

# Bloqueios ativos. Lista vazia significa sem bloqueio.
# Q1 (acesso ao Git) foi decidida na spec — ver D-Q1 (binário + parser puro).
blocked_by: []

# Questões já resolvidas.
# Q1–Q5 e as decisões D-Q6/D-Q7 estão na seção "Decisões de escopo" da spec.
resolved_questions: []

# Contadores derivados de tasks.md. total = pending + in_progress + done.
# Incrementos 1, 2 e 3 (10 tarefas).
tasks:
  total: 10
  pending: 0
  in_progress: 0
  done: 10
