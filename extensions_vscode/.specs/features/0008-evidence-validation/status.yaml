# Estado da mudança. Forma válida: plugins/sdd-kit/schemas/status.schema.json
#
# Datas SEMPRE entre aspas: sem elas o YAML produz um objeto data e a
# validação por schema falha. Ver standards.md §9.
version: 1

id: "0008-evidence-validation"
# feature | bug | refactor | change
type: "feature"
title: "Evidências e validação da feature"

# Incrementos 1 (validação, RF-017) e 2 (coleta de evidências, RF-016) implementados.
# Segue IN_PROGRESS: REQ-EVID-003 (trava de conclusão) fica como lacuna (D-Q6).
status: VERIFIED

created: "2026-07-31"
updated: "2026-08-01"

# Histórico de transições. Só acrescenta; nunca reescreve.
history:
  - status: DRAFT
    date: "2026-07-31"
    reason: >-
      Materializada a partir do backlog (Épico 8) a pedido do usuário, para
      especificar e implementar. spec.md rascunhada a partir dos RF-016 e RF-017 do
      PRD, com as decisões de escopo/design registradas como questões abertas (Q1–Q5).
  - status: IN_PROGRESS
    date: "2026-08-01"
    reason: >-
      Q1–Q4 respondidas pelo autor (D-Q1..D-Q4): primeiro incremento = relatório de
      validação (RF-017) em webview (ADR-012), heurística aprovada, coleta de
      evidências futura sem executar comandos. Incremento 1 implementado, fora da
      ordem formal do fluxo (sem approve/design da Fase 1): núcleos puros
      validationReport.ts e validationHtml.ts, comando "Validar mudança". 7 testes
      novos. Verificação: compile, lint e 105 testes limpos. Transição DRAFT →
      IN_PROGRESS confirmada pelo autor.
  - status: IN_PROGRESS
    date: "2026-08-01"
    reason: >-
      Incremento 2 implementado: coleta de evidências (RF-016, REQ-EVID-002). Reúne
      validação, git (branch/diff), commits (git log, só leitura, D-Q4) e progresso
      num evidence.md, sem sobrescrever conteúdo humano (D-Q5). Núcleos puros
      evidenceDoc.ts e parseLog (gitParse.ts), leitura readCommits (gitAdapter.ts),
      comando "Coletar evidências". 3 testes novos. Verificação: compile, lint e 108
      testes limpos, mais e2e da coleta contra o repo real. REQ-EVID-003 (trava de
      conclusão) fica lacuna: a extensão não executa a conclusão (D-Q6).
  - status: VERIFIED
    date: "2026-08-01"
    reason: >-
      Verificada a pedido do usuário. RF-016 e RF-017 cobertos (a trava de conclusão REQ-EVID-003
      é lacuna consciente, D-Q6), com os comandos Validar/Coletar funcionais na extensão
      empacotada (0.0.7) e instalada. Núcleos puros cobertos por testes; verificação global verde
      (compile, lint, 115 testes). Transição IN_PROGRESS → VERIFIED fora da ordem formal (approval
      segue null; approve é da Fase 2), confirmada pelo usuário.

# null significa NÃO APROVADO. Preenchido por /sdd-kit:approve com
# {date, by, revision}.
approval: null

# Bloqueios ativos. Lista vazia significa sem bloqueio.
blocked_by: []

# Questões já resolvidas.
# Q1–Q6 estão na seção "Decisões de escopo" da spec.
resolved_questions: []

# Contadores derivados de tasks.md. total = pending + in_progress + done.
tasks:
  total: 6
  pending: 0
  in_progress: 0
  done: 6
