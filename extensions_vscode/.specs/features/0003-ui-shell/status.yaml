# Estado da mudança. Forma válida: plugins/sdd-kit/schemas/status.schema.json
#
# Datas SEMPRE entre aspas: sem elas o YAML produz um objeto data e a
# validação por schema falha. Ver standards.md §9.
version: 1

id: "0003-ui-shell"
type: "feature"
title: "Interface — dashboard da feature e editor de especificações"

status: DRAFT

created: "2026-07-31"
updated: "2026-07-31"

history:
  - status: DRAFT
    date: "2026-07-31"
    reason: >-
      Materializada a partir do backlog (Épico 3, RF-005/RF-006), iniciando o
      próximo épico após 0001 e 0002 (VERIFIED). Spec inicial rascunhada com o
      escopo do primeiro incremento no dashboard da feature (RF-005, somente
      leitura); o editor visual (RF-006) fica para o incremento seguinte, atrelado
      a Q1 (webview vs. custom editor). Segue em DRAFT — requisitos a detalhar e
      três questões em aberto (Q1 alta).

# null significa NÃO APROVADO. Preenchido por /sdd-kit:approve com
# {date, by, revision}.
approval: null

# Bloqueios ativos. Lista vazia significa sem bloqueio.
blocked_by: []

# Questões já resolvidas. Preservadas em vez de apagadas.
resolved_questions: []

# Contadores derivados de tasks.md. total = pending + in_progress + done.
# tasks.md será gerado por /sdd-kit:tasks a partir da spec detalhada.
tasks:
  total: 0
  pending: 0
  in_progress: 0
  done: 0
