# Estado da mudança. Forma válida: plugins/sdd-kit/schemas/status.schema.json
#
# Datas SEMPRE entre aspas: sem elas o YAML produz um objeto data e a
# validação por schema falha. Ver standards.md §9.
version: 1

id: "0010-publishing"
# feature | bug | refactor | change
type: "feature"
title: "Publicação no Marketplace"

# Parte automatizável (empacotamento, metadados, README, pipeline) implementada.
# Segue IN_PROGRESS: a publicação em si é ato manual do autor (ícone/PAT/Release).
status: IN_PROGRESS

created: "2026-08-01"
updated: "2026-08-01"

# Histórico de transições. Só acrescenta; nunca reescreve.
history:
  - status: DRAFT
    date: "2026-08-01"
    reason: >-
      Materializada a partir do backlog (Épico 10) a pedido do usuário, para
      preparar a extensão para o Marketplace. spec.md rascunhada; a publicação em si
      é ato do autor (conta/PAT/ícone), fora do escopo desta mudança. Questões
      abertas Q1–Q3; nenhuma crítica.
  - status: IN_PROGRESS
    date: "2026-08-01"
    reason: >-
      Q1–Q3 respondidas pelo autor (D-Q1..D-Q3): publicação em GitHub Release, para
      Marketplace + Open VSX, README com badges. Incremento 1 implementado, fora da
      ordem formal do fluxo (sem approve/design da Fase 1): .vscodeignore reforçado e
      metadados no package.json (bugs/homepage/qna/galleryBanner), README revisado
      como página do Marketplace com checklist de passos manuais, e workflow
      publish.yml (release → vsce + ovsx, protegido por VSCE_PAT/OVSX_PAT).
      Verificação: `vsce ls` (pacote sem .specs/src/testes), package.json e
      publish.yml válidos. Transição DRAFT → IN_PROGRESS confirmada pelo autor.
      A publicação (ícone PNG, conta/PAT, disparar o Release) encerra o épico.

# null significa NÃO APROVADO. Preenchido por /sdd-kit:approve com
# {date, by, revision}.
approval: null

# Bloqueios ativos. Lista vazia significa sem bloqueio.
blocked_by: []

# Questões já resolvidas.
# Q1–Q3 estão na seção "Decisões de escopo" da spec.
resolved_questions: []

# Contadores derivados de tasks.md. total = pending + in_progress + done.
tasks:
  total: 3
  pending: 0
  in_progress: 0
  done: 3
