# Estado da mudança. Forma válida: plugins/sdd-kit/schemas/status.schema.json
#
# Datas SEMPRE entre aspas: sem elas o YAML produz um objeto data e a
# validação por schema falha. Ver standards.md §9.
version: 1

id: "0030-board-sorting"
# feature | bug | refactor | change
type: "feature"
title: "Ordenação do quadro e do feed"

# Verificada em 2026-08-04: ordenação testada; render no host.
# approve segue pulado (Fase 2).
status: VERIFIED

created: "2026-08-04"
updated: "2026-08-04"

# Histórico de transições. Só acrescenta; nunca reescreve.
history:
  - status: DRAFT
    date: "2026-08-04"
    reason: >-
      Criada por /sdd-kit:new: ordenar os cartões do quadro (id/título/progresso) e alternar a ordem do
      feed (mais recentes/mais antigos) no Painel SDD (0025). Lógica pura como no filtro (0028).
      Questões de design em TASK-SORT-001 (ADR-028).
  - status: PLANNED
    date: "2026-08-04"
    reason: >-
      Plano gerado por /sdd-kit:tasks (3 tarefas P; caminho crítico TASK-SORT-001 → 002 → 003).
      CLARIFIED/DESIGNED pulados (Fase 2): design em ADR-028. Cobertura completa (4 requisitos com
      tarefa); sem órfã, ciclo ou tarefa G.
  - status: IN_PROGRESS
    date: "2026-08-04"
    reason: >-
      Implementado. boardModel.ts: BoardSort/FeedOrder, sortBoardCards (id-asc/id-desc/title/progress,
      não muta) e orderFeed (desc/asc). boardHtml.ts: state.sort/feedOrder, <select> na barra (espelha
      sortCards) aplicado após o filtro em renderBoardArea, e alternador na visão Atividade; controles
      com aria-label. Transição PLANNED → IN_PROGRESS; approve pulado (Fase 2), approval segue null.
  - status: VERIFIED
    date: "2026-08-04"
    reason: >-
      Verificação por evidência: compile e lint limpos; suíte unitária 163 (+2): TEST-SORT-001
      (sortBoardCards por id asc/desc, título e progresso — sem tarefas por último —, sem mutar a
      entrada; SCN-SORT-001) e TEST-SORT-002 (orderFeed inverte para asc e preserva desc; SCN-SORT-002).
      Ordenação pura, sem vscode (NFR-SORT-001). Os controles e o render ordenado são integração de
      host (NFR-SORT-002), registrados em traceability.yaml gaps com revisão manual; abrir o painel é
      coberto pelo E2E do 0025. Critérios de aceite atendidos. approve permanece pulado (Fase 2).

# null significa NÃO APROVADO. Preenchido por /sdd-kit:approve com
# {date, by, revision}.
approval: null

# Bloqueios ativos. Lista vazia significa sem bloqueio.
blocked_by: []

# Questões já resolvidas.
resolved_questions:
  - question: Q1
    date: "2026-08-04"
    resolved_by: TASK-SORT-001
    summary: >-
      Ordenação client-side espelhando sortBoardCards/orderFeed (puros, testados); <select> para o
      quadro (após o filtro), alternador para o feed.
    adr: ADR-028

# Contadores derivados de tasks.md. total = pending + in_progress + done.
tasks:
  total: 3
  pending: 0
  in_progress: 0
  done: 3
